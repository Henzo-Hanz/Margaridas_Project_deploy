# Margarida's Garden - Variáveis de Ambiente
# Copie este arquivo para .env e preencha os valores

# OBRIGATÓRIO em produção - gere com: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=

# Banco de dados (PostgreSQL para produção/Vercel; SQLite para desenvolvimento)
# Exemplo SQLite: sqlite:///./backend/gardendb.sqlite
# Exemplo Postgres: postgresql://user:pass@host:5432/dbname
DATABASE_URL=

# CORS - origens permitidas separadas por vírgula (ex: http://localhost:5173,https://app.exemplo.com)
# Em desenvolvimento: http://localhost:3000,http://localhost:5173
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:5173

# Ambiente: development | production
ENVIRONMENT=development

# Usuário padrão (usado pelo init_db se tabela vazia)
DEFAULT_USER_NAME=Margarida
DEFAULT_USER_EMAIL=margarida@garden.local
DEFAULT_USER_PASSWORD=
